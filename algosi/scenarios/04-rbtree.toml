[tasks]
[tasks.rbtree]
algorithm = "rb_tree"
debug = false
data = [10, 20, 15, 18, 19, 30, 35, 37, 40, 38, 39, 34, 31, 33]
script = """
function try_find_node(n)
	local number = tree:find(n)
	if number == nil then
		print("Число " .. tostring(n) .. " не найдено")
	else
		print("Число " .. tostring(n) .. " найдено")
	end
end

tree:print()
tree:remove(18)
tree:print()
tree:remove(35)
tree:print()
tree:remove(34)
tree:print()
tree:remove(39)
tree:print()

--try_find_node(69)
--try_find_node(42)

--tree:print()
--tree:insert(42)
--tree:print()
--tree:remove(54)
--tree:print()

--try_find_node(69)
--try_find_node(42)

"""